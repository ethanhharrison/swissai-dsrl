diffusers==0.33.1
distrax==0.1.5
dm-control==1.0.14
easydict==1.13
einops==0.8.1
flax==0.10.2
future==1.0.0
gym==0.26.2
gym-notices==0.0.8
gymnasium==1.0.0
h5py==3.13.0
imageio==2.37.0
# bumped from 0.5.0 to match upstream openpi (jax[cuda12] installed separately below in README)
jax==0.5.3
ml_collections==1.0.0
ml_dtypes==0.5.1
optax==0.2.4
optree==0.15.0
# bumped from 0.11.1 to match upstream openpi
orbax-checkpoint==0.11.13
# bumped from 5.4.0 to satisfy deepdiff 8.6.0 (>=5.4.1, <6); still 5.4.x.
orderly-set==5.4.1
packaging==24.2
pandas==2.2.3
pillow==10.4.0
scipy==1.15.2
tensorflow==2.19.0
etils==1.12.2
matplotlib==3.9.3
wandb[media]==0.19.9
opencv-python==4.11.0.86
robosuite==1.4.1
bddl==3.5.0
tf_keras==2.19.0
moviepy==1.0.3

# --- openpi runtime dependencies ---
# Pre-installed here so `pip install -e openpi --no-deps` can skip resolution
# (upstream openpi uses uv with `{ path = ..., editable = true }` / git sources
#  that pip cannot respect, which triggers `resolution-too-deep`).
augmax>=0.3.4
beartype==0.19.0
chex==0.1.90
dm-tree>=0.1.8
equinox>=0.11.8
flatbuffers>=24.3.25
# pinned to 2024.3.1 in constraints.txt (datasets 2.19.0 caps fsspec<=2024.3.1;
# lerobot's data pipeline goes through datasets, so openpi's >=2024.6.0 hint
# is overridden). gcsfs is reinstalled at the matching version in the README.
fsspec[gcs]
jaxtyping==0.2.36
numpydantic>=1.6.6
polars>=1.30.0
rich>=14.0.0
sentencepiece>=0.2.0
tqdm-loggable>=0.2
transformers==4.53.2
treescope>=0.1.7
typing-extensions>=4.12.2
tyro>=0.9.5
filelock>=3.16.1

# --- openpi-client runtime dependencies ---
msgpack>=1.0.5
tree>=0.2.4
websockets>=11.0

# --- lerobot runtime dependencies ---
# openpi.training.data_loader imports lerobot at module scope, so lerobot
# must be importable even when we only run inference. Installing lerobot
# itself (pinned git rev) with --no-deps is done in the README; these are
# the subset of its runtime deps that actually fire during `import lerobot`.
# Heavy/optional deps (mujoco envs, cameras, real-robot drivers) are NOT
# installed here and will only matter if those submodules are touched.
datasets==2.19.0
pyarrow==16.1.0
pyarrow_hotfix
dill==0.3.8
multiprocess==0.70.16
deepdiff==8.6.0
torchcodec==0.2.1
av>=14.2.0
rerun-sdk>=0.21.0
jsonlines>=4.0.0
draccus==0.10.0
flask>=3.0.3
gdown>=5.1.0
pyzmq>=26.2.1
omegaconf>=2.3.0
termcolor
zarr
pymunk
